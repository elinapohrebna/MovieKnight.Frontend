import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  paper: {
    padding: "20px 20px",
    maxWidth: 780,
    width: "100%",
    margin: "10px auto",
  },
  button: {
    marginTop: 10,
  },
});