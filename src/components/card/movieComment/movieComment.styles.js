import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  paper: {
    padding: "20px 20px",
    width: 500,
    margin: "10px auto",
  },
  button: {
    marginTop: 10,
  },
});
